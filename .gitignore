# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.eggs/
*.egg-info/
dist/
build/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtual environments
.venv/
env/
venv/
.envrc

# macOS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Claude Code
.claude/

# Ralph tasks
tasks/

# ML / PyTorch model weights
*.pt
*.pth
*.ckpt
*.safetensors
sts_models/

# Logs / training runs
*.log
v7_run*_logs/
runs/
wandb/
mlruns/

# V7/V8 bot 临时游戏日志（根目录跑出来的 per-seed jsonl）
/v7_game_log_*.jsonl
/v8_game_log_*.jsonl

# Jupyter
.ipynb_checkpoints/

# Environment variables
.env
.env.*

# Archived old versions (V3/V4/V5)
archive/

# Standalone demo files
demos/

# Utility scripts
tools/

# MNIST dataset (re-downloadable)
data/MNIST/

# Local work / scratch
*.local
scratch/

# 外部 clone（仅参考用，不入库）
# 注：不能直接写 `external/`，否则其下的 sts_lightspeed_patches/ 无法用 ! 反选回来
# （git 规则：父目录被整体排除后无法再 re-include 子文件）。改为只排除已知的子目录。
external/*
# 例外：sts_lightspeed 本地补丁记录必须入库（防重 clone 丢失，见该目录 README）
!external/sts_lightspeed_patches/

# web/ 数据产物（不入 git）
web/data/*.sqlite
web/data/localization_zho.json

# workflow 运行的一次性工件（不入 git）
workflow_state/

# smoke 测试产物 / 本地日志（不入 git）
data/v8_smoke/
data/v8_smoke_resume_base/
data/v8_smoke_resume_test/
logs_smoke_stderr.txt

# Rust 引擎持久 worktree（不追踪，避免污染 git status）
external/stsrl_rust_main/

external/sts_lightspeed/
